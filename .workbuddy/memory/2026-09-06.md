# 2026-09-06

## Scrum Master Agent 项目搭建

从零搭出 `scrumMasterAgent` 骨架，目标：把每日 DSU 会议 → 结构化 JSON → 每 pod 一份静态 HTML，托管在 GitHub Pages。

关键决策：
- GitHub 仓库同时当数据库 + 托管平台，不引入额外 DB/对象存储。`pods/<pod>/dsu/<date>.html` + `raw/<date>.json`，`data/dsu-index.json` 供前端检索。
- 后端 Python(FastAPI)，流水线四步：ingest → extract(LLM) → enrich(Jira/GitHub MCP) → render(Jinja2) → publish(GitHub MCP push_files 批量提交)。
- MCP 用 `mcp` Python SDK 的 stdio 客户端，配置沿用标准 `mcpServers` JSON（与 `~/.workbuddy/mcp.json` 同构），`${VAR}` 从环境变量展开。
- 发布默认走人工确认闸门（`REQUIRE_HUMAN_APPROVAL`），草稿先进 `.workdir`，确认后才 commit。Jira 写操作默认关闭。
- mcp 依赖在 enrich/publish 里延迟导入，保证没装 mcp 时本地渲染链路仍可跑。

已验证（无 LLM/无 MCP 的降级路径）：
`python -m app.cli generate --pod payments-pod --date 2026-09-06 --no-mcp` 成功产出
`.workdir/pods/payments-pod/dsu/2026-09-06.html` 与 raw JSON，规则切分按 人: 内容 分桶，阻塞识别用 "阻塞：" 前缀优先。

全链路已验证（装齐 fastapi/uvicorn/mcp/openai/apscheduler 后）：
- `import app.main, app.scheduler, app.mcp_client, app.cli` 全部 OK
- scheduler 注册正确：payments-pod 周一至周五 09:45、growth-pod 10:00（会议时间 +15min）
- uvicorn 起服务后 `/api/pods` 返回两个 pod，`/api/dsu/payments-pod` 返回结构化记录，
  `/dsu/payments-pod/2026-09-06` 返回 200 / 6814 bytes 静态报告

待办：真实转录接入（Zoom/Teams/飞书）、LLM prompt 调优、前端部署到 Pages、Slack 审批卡片。
